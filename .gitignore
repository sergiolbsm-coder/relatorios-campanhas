# Segredos — nunca commitar
.env
secrets/
*.json
!package.json
!docs/**/*.json

# Python
__pycache__/
*.pyc
.venv/
venv/

# Dados extraídos localmente
etl/data/
*.csv
!docs/**/*.csv

# Banco local (SQLite) — schema.sql fica versionado, o arquivo de dados não
db/*.db

# Sistema
.DS_Store
